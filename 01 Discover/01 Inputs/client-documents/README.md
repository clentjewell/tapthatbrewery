# Client documents — received 19 August 2026

Five documents forwarded by Tap That Brewery after the discovery session, via Google Drive
(folder `1dDgxzMojCRBYyk_qNIiU4o4G7IpymBMn`, owner clent@jewellprojects.com).

Each is stored as the original `.docx` plus a `.txt` conversion (headings, paragraphs and tables
extracted with `python-docx`) so the content is greppable and diffable.

| Document | Size | What it is |
|---|---|---|
| `Census - Raw Data.docx` | 22k chars | The raw customer census tabulations — **49 respondents from 206 keg system owners (24%)**. The factual ground truth. |
| `TAP THAT CENSUS FINDINGS - cgpt.docx` | 97k chars | Analysed write-up of the census. |
| `TT GROWTH MASTER PLAYBOOK.docx` | 99k chars | The client's own growth plan. |
| `Scaling Up - complete.docx` | 49k chars | Scaling / operations plan. |
| `Future of Tap That.docx` | 45k chars | Founders' vision document. |

## Why this matters

These arrived **after** the 65-document 3D Process set was drafted. That set was built from one
discovery session and a site visit, and it carried the census as open item #1 — the largest
blocker to CP1 sign-off. This closes it, and in doing so corrects several load-bearing
assumptions.

**Read `../../02 Working Drafts/23A__Census-Reconciliation__…` before trusting any figure in the
earlier drafts.** It records what changed, what it changed, and which documents were revised.

## Provenance note

The census write-up filename carries a `- cgpt` suffix, indicating it was analysed with an AI
tool. Where the write-up and the raw tabulations disagree, **the raw data wins** — the
reconciliation document records any such conflicts.
